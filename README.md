Suvikash website (static)

Quick start:

- Open `index.html` in your browser (double-click or use a local server).
- Edit contact details and address in `index.html`.
- Add images to an `images/` folder and update markup where needed.

Next suggestions:

- Add real logos and photos (optimize for web).
- Hook contact form to an email service or backend endpoint.
- Deploy to GitHub Pages or any static host.
